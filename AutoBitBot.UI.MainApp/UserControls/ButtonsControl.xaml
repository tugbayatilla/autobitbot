<UserControl x:Class="AutoBitBot.UI.MainApp.UserControls.ButtonsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal">
        <Button Command="{Binding Open_BittrexSellLimitCommand}" CommandParameter="{Binding}">Sell</Button>
        <Separator />
        <Button Command="{Binding Open_BittrexBuyLimitCommand}" CommandParameter="{Binding}">Buy</Button>
        <Separator />
        <Button Command="{Binding Open_BittrexBuyAndSellLimitCommand}" CommandParameter="{Binding}">Buy&amp;Sell</Button>

    </StackPanel>
</UserControl>

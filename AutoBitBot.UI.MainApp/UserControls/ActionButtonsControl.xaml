<UserControl x:Class="AutoBitBot.UI.MainApp.UserControls.ActionButtonsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutoBitBot.UI.MainApp.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="75" d:DesignWidth="500">
    
    <StackPanel Orientation="Horizontal">
        <Button CommandParameter="{Binding}" Command="{Binding Path=OpenBuyAndSellCommand}" Style="{DynamicResource MetroButtonStyle}">Open BuyAndSell</Button>
        <Button CommandParameter="{Binding}" Command="{Binding Path=OpenMarketsCommand}" Style="{DynamicResource MetroButtonStyle}">Open Markets</Button>
        <Button CommandParameter="{Binding}" Command="{Binding Path=OpenKilledTasksCommand}" Style="{DynamicResource MetroButtonStyle}">Open Killed Tasks</Button>
        <Button CommandParameter="{Binding}" Command="{Binding Path=OpenOrderHistoryCommand}" Style="{DynamicResource MetroButtonStyle}">Open OrderHistory</Button>
    </StackPanel>
</UserControl>
